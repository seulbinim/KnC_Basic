{
  "scripts": {
    "start": "run-p sass serve",
    "sass": "sass src/scss:src/css --watch",
    "serve": "live-server src/"
  },
  "devDependencies": {
    "live-server": "^1.2.2",
    "npm-run-all": "^4.1.5",
    "sass": "^1.54.5"
  }
}
